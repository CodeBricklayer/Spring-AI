spring:
  application:
    name: ai-code-helper

langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-max
        api-key: ${OPENAI_API_KEY}
      embedding-model:
        api-key: ${OPENAI_API_KEY}
        model-name: text-embedding-v4
      streaming-chat-model:
        model-name: qwen-max
        api-key: ${OPENAI_API_KEY}
bigmodel:
  api-key: ${ZHIPU_API_KEY}

server:
  port: 8081
  servlet:
    context-path: /api